export default {} as {
  /** an average-sized heading */
  "h": string;
  /** the 1st largest heading size */
  "h1": string;
  /** the 2nd largest heading size */
  "h2": string;
  /** the 3rd largest heading size */
  "h3": string;
  /** the 4th largest heading size */
  "h4": string;
  /** the 5th largest heading size */
  "h5": string;
  /** the smallest heading size */
  "h6": string;
  /** an avrarge-sized inline text */
  "text": string;
  /** an avrarge-sized paragraph */
  "para": string;
  /** large font size for elements slected by `.text`, `.para`, and `p` */
  "lg": string;
  /** medium font size for elements slected by `.text`, `.para`, and `p` */
  "md": string;
  /** small font size for elements slected by `.text`, `.para`, and `p` */
  "sm": string;
  /** extra small font size for elements slected by `.text`, `.para`, and `p` */
  "xs": string;
  /** extra extra small font size for elements slected by `.text`, `.para`, and `p` */
  "xxs": string;
  /** a small garyed-out inline piece of text */
  "caption": string;
  /** by default, links display as an ordenary text. to style them as links, use this class */
  "link": string;
  /** hides text from screen but keeps it visible to screen readers */
  "screen-reader-text": string;
  /** a horizontal rule */
  "hr": string;
  /** underlined text */
  "ul": string;
  /** overlined text */
  "ol": string;
  /** line-through-ed text */
  "lt": string;
  /** italic text */
  "it": string;
  /** center-aligned text */
  "center": string;
  /** start-aligned text */
  "start": string;
  /** end-aligned text */
  "end": string;
  /** left-aligned text */
  "left": string;
  /** right-aligned text */
  "right": string;
};  