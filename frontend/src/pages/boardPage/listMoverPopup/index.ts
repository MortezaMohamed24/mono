/**
 * i exported same values with multiple names to make it easier for importers
 * to import them without the need for renaming on thier part
 */

import "./types";

export {ID} from "./constants";
export {ID as BOARD_LIST_MOVER_POPUP_ID} from "./constants";

export {default as default} from "./component";
export {default as BoardListMoverPopup} from "./component";