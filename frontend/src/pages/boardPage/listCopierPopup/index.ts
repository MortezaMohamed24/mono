/**
 * i exported same values with multiple names to make it easier for importers
 * to import them without the need for renaming
 */

import "./types";

export {ID as ID} from "./constants";
export {ID as BOARD_LIST_COPIER_POPUP_ID} from "./constants";

export {default as default} from "./component";
export {default as BoardListCopierPopup} from "./component";