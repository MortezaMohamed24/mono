export type INVALID_BODY = typeof INVALID_BODY;
export type INVALID_QUERY = typeof INVALID_QUERY;


export const INVALID_BODY = "gate: invalid body";
export const INVALID_QUERY = "gate: invalid query";


export default Object.freeze({
  INVALID_BODY,
  INVALID_QUERY,
});