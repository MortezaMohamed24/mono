export type NOT_FOUND = typeof NOT_FOUND
export type INVALID_NAME = typeof INVALID_NAME
export type INVALID_COLOR = typeof INVALID_COLOR
export type INVALID_PROJECTOR = typeof INVALID_PROJECTOR


export const NOT_FOUND = "label: could not find label"
export const INVALID_NAME = "label: invalid name"
export const INVALID_COLOR = "label: invalid color"
export const INVALID_PROJECTOR = "label: invalid projector"


export default Object.freeze({
  NOT_FOUND,
  INVALID_NAME,
  INVALID_COLOR,
})